db:
  image: postgres
  volumes:
    - ./volumes/data:/var/lib/postgresql/data
  expose:
    - "5432:5432"
web:
  build: .
  ports:
    - "20022:22"
    - "80:80"
  volumes:
    - .:/code
  links:
    - db
